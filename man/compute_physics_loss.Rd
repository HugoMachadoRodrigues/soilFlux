% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physics.R
\name{compute_physics_loss}
\alias{compute_physics_loss}
\title{Compute the physics-informed residual loss (Norouzi et al. 2025)}
\usage{
compute_physics_loss(
  theta_model,
  res_tensors,
  lambda3 = 1,
  lambda4 = 1000,
  lambda5 = 1000,
  lambda6 = 1,
  pf_left = -2,
  pf_right = 7.6,
  training = TRUE
)
}
\arguments{
\item{theta_model}{A Keras model with two inputs: \code{Xseq} and \code{pf_norm}.}

\item{res_tensors}{A named list with four sublists (\code{set1} – \code{set4}),
each containing \code{Xseq} and \code{pf} tensors (output of
\code{\link[=residual_to_tensors]{residual_to_tensors()}}).}

\item{lambda3}{Weight for S1 linearity loss (default 1.0).}

\item{lambda4}{Weight for S2 non-negativity loss (default 1000.0).}

\item{lambda5}{Weight for S3 non-positivity loss (default 1000.0).}

\item{lambda6}{Weight for S4 saturation loss (default 1.0).}

\item{pf_left}{Left boundary of the normalised pF domain (default -2).}

\item{pf_right}{Right boundary (default 7.6).}

\item{training}{Logical passed to the model call (default \code{TRUE}).}
}
\value{
A named list of TensorFlow scalars:
\code{L_phys}, \code{L_lin}, \code{L_pos}, \code{L_neg}, \code{L_sat}.
}
\description{
Evaluates four physics constraints against the current model weights:
\describe{
\item{S1 (L_lin)}{Second derivative \eqn{|\partial^2\theta/\partial pF^2|}
in the dry end \eqn{pF \in [5, 7.6]} should be near zero (linearity).}
\item{S2 (L_pos)}{\eqn{\theta(pF = 6.2) \geq 0} (non-negativity).}
\item{S3 (L_neg)}{\eqn{\theta(pF = 7.6) \leq 0} (non-positivity).}
\item{S4 (L_sat)}{First derivative \eqn{|\partial\theta/\partial pF|}
near zero in the saturated plateau \eqn{pF \in [-2, -0.3]}.}
}
}
\keyword{internal}
