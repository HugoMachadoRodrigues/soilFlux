% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{make_obs_matrices}
\alias{make_obs_matrices}
\title{Build observation matrices for the CNN1D model}
\usage{
make_obs_matrices(
  df,
  x_inputs,
  scaler,
  K = 64L,
  knot_grid = seq(0, 1, length.out = K),
  pf_left = -2,
  pf_right = 7.6,
  wet_split_cm = 4.2,
  w_wet = 1,
  w_dry = 1
)
}
\arguments{
\item{df}{A prepared data frame (output of \code{\link[=prepare_swrc_data]{prepare_swrc_data()}}
or compatible structure) containing covariate columns, \code{matric_head},
\code{theta_n}, and \code{theta_max_n}.}

\item{x_inputs}{Character vector of covariate column names.}

\item{scaler}{A fitted scaler from \code{\link[=fit_minmax]{fit_minmax()}}.}

\item{K}{Number of knot points (default \code{64L}).}

\item{knot_grid}{Numeric vector of knot positions (default
\code{seq(0, 1, length.out = K)}).}

\item{pf_left}{Left boundary of the pF domain (default \code{-2}).}

\item{pf_right}{Right boundary of the pF domain (default \code{7.6}).}

\item{wet_split_cm}{Matric head threshold (cm) separating wet / dry end
(default \code{4.2}, corresponding to pF approximately 0.62).}

\item{w_wet}{Sample weight for wet-end observations (default \code{1}).}

\item{w_dry}{Sample weight for dry-end observations (default \code{1}).}
}
\value{
A named list:
\describe{
\item{\code{Xseq}}{3-D numeric array [N, K, p+1].}
\item{\code{pf}}{Numeric matrix [N, 1] of normalised pF values.}
\item{\code{y}}{Numeric matrix [N, 2]: columns are \code{theta_n} and
\code{theta_max_n}.}
\item{\code{w}}{Numeric vector of sample weights.}
}
}
\description{
Converts a prepared data frame into the arrays needed for training or
evaluation: a 3-D sequence array \code{Xseq} (shape \verb{[N, K, p+1]}) and
companion vectors \code{pf}, \code{y}, and sample weights \code{w}.
}
\keyword{internal}
