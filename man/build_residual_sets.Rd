% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/physics.R
\name{build_residual_sets}
\alias{build_residual_sets}
\title{Build physics residual point sets (S1 – S4)}
\usage{
build_residual_sets(
  df_raw,
  x_inputs,
  S1 = 1500L,
  S2 = 500L,
  S3 = 500L,
  S4 = 1500L,
  pF_lin_min = 5,
  pF_lin_max = 7.6,
  pF0_pos = 6.2,
  pF1_neg = 7.6,
  pF_sat_min = -2,
  pF_sat_max = -0.3,
  seed = 123L
)
}
\arguments{
\item{df_raw}{Data frame with covariate columns (training split).}

\item{x_inputs}{Character vector of covariate column names.}

\item{S1}{Number of S1 points — dry-end linearity (default 1500).}

\item{S2}{Number of S2 points — non-negativity at pF0 (default 500).}

\item{S3}{Number of S3 points — non-positivity at pF1 (default 500).}

\item{S4}{Number of S4 points — saturated plateau (default 1500).}

\item{pF_lin_min}{Lower pF for the S1 linearity constraint (default 5.0).}

\item{pF_lin_max}{Upper pF for the S1 linearity constraint (default 7.6).}

\item{pF0_pos}{pF at which theta must be >= 0 — S2 (default 6.2).}

\item{pF1_neg}{pF at which theta must be <= 0 — S3 (default 7.6).}

\item{pF_sat_min}{Lower pF for the S4 plateau constraint (default -2.0).}

\item{pF_sat_max}{Upper pF for the S4 plateau constraint (default -0.3).}

\item{seed}{Integer random seed (default 123).}
}
\value{
A named list with four data frames: \code{set1}, \code{set2}, \code{set3},
\code{set4}. Each data frame has one row per collocation point, with columns
corresponding to \code{x_inputs} (sampled uniformly within training range)
and a \code{pF} column.
}
\description{
Generates four sets of collocation points (random soil-property vectors
and associated pF values) used to evaluate the physics constraints during
training.
}
\examples{
\donttest{
sets <- build_residual_sets(train_df, c("clay","silt","sand_total","Depth_num"))
}

}
