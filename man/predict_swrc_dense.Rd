% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/predict.R
\name{predict_swrc_dense}
\alias{predict_swrc_dense}
\title{Predict dense SWRC curves for a set of soil profiles}
\usage{
predict_swrc_dense(
  object,
  newdata,
  n_points = 1000L,
  pf_range = NULL,
  id_cols = c("PEDON_ID", "Depth_num", "Depth_label", "Texture")
)
}
\arguments{
\item{object}{A \code{swrc_fit} object.}

\item{newdata}{A data frame with covariate columns plus (optionally)
\code{PEDON_ID}, \code{Depth_num}, \code{Depth_label}, and \code{Texture}.}

\item{n_points}{Number of equally spaced pF points (default \code{1000}).}

\item{pf_range}{Numeric vector of length 2: min and max pF values for
the output grid (default \code{c(-2, 7.6)}).}

\item{id_cols}{Character vector of columns used to identify profiles
(default \code{c("PEDON_ID","Depth_num","Depth_label","Texture")}).}
}
\value{
A tibble with columns: all \code{id_cols} present in \code{newdata},
\code{pF}, \code{matric_head}, and \code{theta} (predicted volumetric water content
in m3/m3).
}
\description{
For each unique (PEDON_ID × depth) profile in \code{newdata}, predicts theta
across a dense grid of pF values and returns a tidy long-format tibble.
}
\examples{
\dontrun{
dense <- predict_swrc_dense(fit, newdata = test_df, n_points = 500)
}

}
