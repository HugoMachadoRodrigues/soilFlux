% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plots.R
\name{plot_swrc}
\alias{plot_swrc}
\title{Plot soil water retention curves (SWRC)}
\usage{
plot_swrc(
  pred_curves,
  obs_points = NULL,
  curve_col = "theta",
  obs_col = "theta_n",
  group_col = "PEDON_ID",
  facet_row = NULL,
  facet_col = NULL,
  x_limits = NULL,
  y_limits = c(-0.25, 7.75),
  line_colour = "steelblue4",
  point_colour = "black",
  base_size = 12,
  title = NULL
)
}
\arguments{
\item{pred_curves}{A data frame (or tibble) of dense curve predictions,
typically returned by \code{\link[=predict_swrc_dense]{predict_swrc_dense()}}.  Must contain columns
\code{pF} and \code{theta}.}

\item{obs_points}{Optional data frame of observed data.  Must contain
\code{pF} and \code{theta} columns (or \code{matric_head} and a theta column).}

\item{curve_col}{Column in \code{pred_curves} for the predicted theta
(default \code{"theta"}).}

\item{obs_col}{Column in \code{obs_points} for observed theta (default
\code{"theta_n"}).}

\item{group_col}{Column name used to distinguish individual profiles
in \code{pred_curves} (default \code{"PEDON_ID"}).}

\item{facet_row}{Column for facet rows (default \code{NULL}).}

\item{facet_col}{Column for facet columns (default \code{NULL}).}

\item{x_limits}{Numeric vector of length 2 for the x-axis (theta)
range (default \code{NULL}, auto).}

\item{y_limits}{Numeric vector of length 2 for the y-axis (pF)
range (default \code{c(-0.25, 7.75)}).}

\item{line_colour}{Colour of the predicted curve lines (default
\code{"steelblue4"}).}

\item{point_colour}{Colour of the observed data points (default
\code{"black"}).}

\item{base_size}{Base font size for \code{theme_bw} (default \code{12}).}

\item{title}{Plot title (default \code{NULL}).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a \code{ggplot2} figure showing continuous SWRC predictions
(lines) optionally overlaid with observed data points.
}
\examples{
\donttest{
pred_curves <- data.frame(
  PEDON_ID = rep(c("P1", "P2"), each = 5),
  pF       = rep(c(0, 1, 2, 3, 4), 2),
  theta    = c(0.42, 0.36, 0.28, 0.18, 0.08,
               0.38, 0.32, 0.25, 0.16, 0.07)
)
plot_swrc(pred_curves, group_col = "PEDON_ID")
}

}
