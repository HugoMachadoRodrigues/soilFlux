% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{fit_swrc}
\alias{fit_swrc}
\title{Fit a physics-informed CNN1D SWRC model}
\usage{
fit_swrc(
  train_df,
  x_inputs,
  val_df = NULL,
  hidden = c(128L, 64L),
  dropout = 0.1,
  lr = 0.001,
  epochs = 80L,
  batch_size = 256L,
  patience = 5L,
  K = 64L,
  lambdas = norouzi_lambdas("norouzi"),
  S1 = 1500L,
  S2 = 500L,
  S3 = 500L,
  S4 = 1500L,
  pF_lin_min = 5,
  pF_lin_max = 7.6,
  pF0_pos = 6.2,
  pF1_neg = 7.6,
  pF_sat_min = -2,
  pF_sat_max = -0.3,
  wet_split_cm = 4.2,
  w_wet = 1,
  w_dry = 1,
  pf_left = -2,
  pf_right = 7.6,
  seed = 123L,
  verbose = TRUE
)
}
\arguments{
\item{train_df}{Data frame for training (output of
\code{\link[=prepare_swrc_data]{prepare_swrc_data()}}).}

\item{x_inputs}{Character vector of covariate column names.}

\item{val_df}{Optional validation data frame (same structure as
\code{train_df}). If \code{NULL}, early stopping is skipped.}

\item{hidden}{Integer vector of length 2: Conv1D filter counts
(default \code{c(128L, 64L)}).}

\item{dropout}{Dropout rate (default \code{0.10}).}

\item{lr}{Learning rate for the Adam optimizer (default \code{1e-3}).}

\item{epochs}{Maximum number of epochs (default \code{80}).}

\item{batch_size}{Mini-batch size (default \code{256}).}

\item{patience}{Early-stopping patience in multiples of 5 epochs
(default \code{5}).}

\item{K}{Number of knot points (default \code{64L}).}

\item{lambdas}{Named list of loss weights; use \code{\link[=norouzi_lambdas]{norouzi_lambdas()}}
to generate (default: \code{norouzi_lambdas("norouzi")}).}

\item{S1, S2, S3, S4}{Residual set sizes (defaults: 1500, 500, 500, 1500).}

\item{pF_lin_min}{Lower pF for S1 linearity constraint (default \code{5.0}).}

\item{pF_lin_max}{Upper pF for S1 linearity constraint (default \code{7.6}).}

\item{pF0_pos}{pF threshold for S2 (default \code{6.2}).}

\item{pF1_neg}{pF threshold for S3 (default \code{7.6}).}

\item{pF_sat_min}{Lower pF for S4 (default \code{-2.0}).}

\item{pF_sat_max}{Upper pF for S4 (default \code{-0.3}).}

\item{wet_split_cm}{Matric head (cm) separating wet/dry end (default \code{4.2}).}

\item{w_wet}{Sample weight for wet observations (default \code{1.0}).}

\item{w_dry}{Sample weight for dry observations (default \code{1.0}).}

\item{pf_left}{Left pF domain boundary (default \code{-2.0}).}

\item{pf_right}{Right pF domain boundary (default \code{7.6}).}

\item{seed}{Random seed (default \code{123}).}

\item{verbose}{Logical; print progress (default \code{TRUE}).}
}
\value{
An S3 object of class \code{swrc_fit}, a named list containing:
\describe{
\item{\code{theta_model}}{The fitted Keras model.}
\item{\code{param_model}}{The theta_s extractor model.}
\item{\code{x_inputs}}{Covariate names used.}
\item{\code{scaler}}{Fitted min-max scaler.}
\item{\code{K}}{Number of knot points.}
\item{\code{dk}}{Knot spacing.}
\item{\code{knot_grid}}{Knot positions in [0, 1].}
\item{\code{pf_left},\code{pf_right}}{pF domain boundaries.}
\item{\code{theta_factor}}{Unit multiplier for theta.}
\item{\code{best_epoch}}{Epoch at which validation loss was minimised.}
\item{\code{lambdas}}{Loss weights used during training.}
\item{\code{history}}{Data frame of per-epoch training/validation losses.}
}
}
\description{
The main user-facing function for training.  Given prepared training and
(optionally) validation data, it builds the model, creates physics
residual sets, runs the training loop with early stopping, and returns
a fitted object for prediction and evaluation.
}
\examples{
\dontrun{
fit <- fit_swrc(train_df, x_inputs = c("clay","silt","bd_gcm3","soc","Depth_num"),
               val_df = val_df, epochs = 80, verbose = TRUE)
}

}
