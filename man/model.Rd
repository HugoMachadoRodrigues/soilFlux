% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/model.R
\name{model}
\alias{model}
\title{CNN1D monotone-integral model architecture}
\description{
Build the physics-informed 1-D CNN with a monotone integral
output layer, as described in Norouzi et al. (2025).
}
\details{
\subsection{Architecture}{

The model takes two inputs:
\enumerate{
\item \code{Xseq_knots}: a 3-D tensor of shape \verb{[N, K, p+1]} — for each
observation, \code{p} scaled covariates are broadcast across \code{K} knot
positions, and the knot positions themselves form the last channel.
\item \code{pf_norm}: a 2-D tensor of shape \verb{[N, 1]} — the query pF value
normalised to \verb{[0, 1]}.
}

The output satisfies:
\deqn{\hat{\theta}(pF) = \theta_s - \int_0^{pF} \text{softplus}(s(t))\,dt}

where \eqn{s(t)} is a 1-channel convolutional output.  Monotone decrease
is guaranteed \strong{by construction} because the integrand is always positive.
}
}
\references{
Norouzi, A. M., et al. (2025). Physics-Informed Neural Networks for
Estimating a Continuous Form of the Soil Water Retention Curve.
\emph{Journal of Hydrology}.
}
