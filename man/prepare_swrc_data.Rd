% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_prep.R
\name{prepare_swrc_data}
\alias{prepare_swrc_data}
\title{Prepare a soil data frame for SWRC modelling}
\usage{
prepare_swrc_data(
  df,
  x_cols = NULL,
  depth_col = "depth",
  fix_bd = TRUE,
  fix_theta = TRUE
)
}
\arguments{
\item{df}{A data frame with soil characterization data.}

\item{x_cols}{Named character vector mapping standard names to
actual column names.  Standard names: \code{"PEDON_ID"}, \code{"sand"},
\code{"silt"}, \code{"clay"}, \code{"soc"}, \code{"bd"}, \code{"matric_head"},
\code{"water_content"}, \code{"depth"}.  Unneeded variables may be omitted.}

\item{depth_col}{Column name for depth (character string, default
\code{"depth"}).  If already parsed, set to \code{NULL}.}

\item{fix_bd}{Logical; apply \code{\link[=fix_bd_units]{fix_bd_units()}} to bulk density
(default \code{TRUE}).}

\item{fix_theta}{Logical; scale theta to m3/m3 if needed (default
\code{TRUE}).}
}
\value{
A tibble with standardised columns plus \code{Depth_num},
\code{Depth_label}, \code{bd_gcm3}, \code{theta_n} (normalised WC), and
\code{theta_max_n} (per-profile maximum theta).
}
\description{
A convenience wrapper that:
\enumerate{
\item Renames columns to standard names.
\item Parses the depth column.
\item Fixes bulk-density units.
\item Detects and normalises volumetric water content units.
\item Computes per-profile maximum theta.
\item Drops rows with missing key variables.
}
}
\examples{
\dontrun{
df_prep <- prepare_swrc_data(raw_df,
  x_cols = c(PEDON_ID = "ID", sand = "sand_pct", ...))
}

}
