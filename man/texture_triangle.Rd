% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texture.R
\name{texture_triangle}
\alias{texture_triangle}
\title{Plot a soil texture triangle (ternary diagram)}
\usage{
texture_triangle(
  df,
  sand_col = "sand_total",
  silt_col = "silt",
  clay_col = "clay",
  color_col = NULL,
  title = "Soil Texture Triangle",
  point_size = 1.5,
  alpha = 0.6
)
}
\arguments{
\item{df}{A data frame.}

\item{sand_col}{Column name for sand (default \code{"sand_total"}).}

\item{silt_col}{Column name for silt (default \code{"silt"}).}

\item{clay_col}{Column name for clay (default \code{"clay"}).}

\item{color_col}{Column name for colouring points (default \code{NULL} for
a single colour).}

\item{title}{Plot title.}

\item{point_size}{Point size (default \code{1.5}).}

\item{alpha}{Point transparency (default \code{0.6}).}
}
\value{
A \code{ggplot} object (or \code{ggtern} object if \code{ggtern} is available).
}
\description{
Creates a ternary diagram coloured by a grouping variable using
\code{ggplot2}.  Requires the \code{ggtern} package (not a hard dependency).
}
\examples{
\donttest{
if (requireNamespace("ggtern", quietly = TRUE)) {
  df <- data.frame(sand_total = c(70, 20, 10),
                   silt = c(15, 50, 30),
                   clay = c(15, 30, 60),
                   Texture = c("Sand", "Silt Loam", "Clay"))
  p <- texture_triangle(df, color_col = "Texture")
}
}

}
