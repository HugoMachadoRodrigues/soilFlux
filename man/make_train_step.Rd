% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/train.R
\name{make_train_step}
\alias{make_train_step}
\title{Create an eager-mode train step function}
\usage{
make_train_step(
  theta_model,
  optimizer,
  lambda_wet = 1,
  lambda_dry = 10,
  wet_split_cm = 4.2,
  lambda3 = 1,
  lambda4 = 1000,
  lambda5 = 1000,
  lambda6 = 1,
  res_tensors,
  pf_left = -2,
  pf_right = 7.6
)
}
\arguments{
\item{theta_model}{Keras model returned by \code{\link[=build_swrc_model]{build_swrc_model()}}\verb{$theta_model}.}

\item{optimizer}{A Keras/TF optimizer (e.g. \code{tf$keras$optimizers$Adam}).}

\item{lambda_wet}{Weight for wet-end data loss (default \code{1.0}).}

\item{lambda_dry}{Weight for dry-end data loss (default \code{10.0}).}

\item{wet_split_cm}{Matric head threshold (cm) separating wet/dry
(default \code{4.2}).}

\item{lambda3}{S1 linearity weight (default \code{1.0}).}

\item{lambda4}{S2 non-negativity weight (default \code{1000.0}).}

\item{lambda5}{S3 non-positivity weight (default \code{1000.0}).}

\item{lambda6}{S4 saturation weight (default \code{1.0}).}

\item{res_tensors}{Named list of physics residual tensors (output of
\code{\link[=residual_to_tensors]{residual_to_tensors()}}).}

\item{pf_left}{Left boundary of pF domain (default \code{-2}).}

\item{pf_right}{Right boundary of pF domain (default \code{7.6}).}
}
\value{
A function \code{f(Xseq, pf_norm_obs, y, sw)} that performs one
gradient step and returns a named list of loss scalars.
}
\description{
Returns a closure that, when called with a mini-batch, computes the
combined data + physics loss and applies gradients.
}
\keyword{internal}
