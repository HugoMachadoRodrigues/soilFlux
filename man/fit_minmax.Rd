% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{fit_minmax}
\alias{fit_minmax}
\title{Fit a min-max scaler from a training data frame}
\usage{
fit_minmax(df, cols)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{cols}{Character vector of column names to include.}
}
\value{
A list with elements:
\describe{
\item{\code{min}}{Named numeric vector of per-column minima.}
\item{\code{rng}}{Named numeric vector of per-column ranges.}
\item{\code{cols}}{The character vector \code{cols} (stored for later use).}
}
}
\description{
Computes per-column minimum and range from \code{df[, cols]}.
Constant columns (range == 0) are assigned a range of 1 to avoid
division by zero.
}
\examples{
df <- data.frame(sand = c(20, 40, 60), clay = c(30, 20, 10))
sc <- fit_minmax(df, c("sand", "clay"))
sc

}
