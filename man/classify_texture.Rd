% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/texture.R
\name{classify_texture}
\alias{classify_texture}
\title{Classify soil texture according to the USDA system}
\usage{
classify_texture(sand, silt, clay, tol = 1)
}
\arguments{
\item{sand}{Numeric vector: sand content (\%).}

\item{silt}{Numeric vector: silt content (\%).}

\item{clay}{Numeric vector: clay content (\%).}

\item{tol}{Tolerance for the 100 \% sum check (default \code{1.0}).}
}
\value{
Character vector of USDA texture class names.  Returns \code{NA}
for rows where values are missing or do not sum to approximately 100.
}
\description{
Returns the USDA texture class name for each row based on the sand, silt,
and clay fractions.  Inputs are expected in per-cent (0–100) and must sum
to approximately 100.
}
\examples{
classify_texture(sand = c(70, 20, 10, 40),
                silt = c(15, 50, 30, 40),
                clay = c(15, 30, 60, 20))

}
