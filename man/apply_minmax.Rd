% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/scale.R
\name{apply_minmax}
\alias{apply_minmax}
\title{Apply a fitted min-max scaler to a data frame}
\usage{
apply_minmax(df, scaler)
}
\arguments{
\item{df}{A data frame or tibble.}

\item{scaler}{A scaler object returned by \code{\link[=fit_minmax]{fit_minmax()}}.}
}
\value{
A numeric matrix scaled to approximately [0, 1] per column.
Columns correspond to \code{scaler$cols}.
}
\description{
Scales \code{df[, scaler$cols]} using the precomputed min/range.
Returns a numeric matrix with the same column order as \code{scaler$cols}.
}
\examples{
df_train <- data.frame(sand = c(20, 40, 60), clay = c(30, 20, 10))
sc       <- fit_minmax(df_train, c("sand", "clay"))
df_new   <- data.frame(sand = c(50, 25), clay = c(15, 28))
apply_minmax(df_new, sc)

}
